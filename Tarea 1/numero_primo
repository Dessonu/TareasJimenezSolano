#La función verifica recibe como parámetro a "string", se verifica que el
#parámetro se un string, sea alfabético y que no sea un espacio vacío.
#Luego se revisa cada caractér y se va invirtiendo.

def invert_case(string):
    finalstring=""
    if type(string)==str:
        if string.isalpha():
            if not(string.isspace()):
                for i in string:
                    if i.isupper():
                        finalstring=finalstring+i.lower()
                    else:
                        finalstring=finalstring+i.upper()         
                print(finalstring)
            else:
                return 
        else:
            return 
    else:
        return 
    
invert_case("P")

# La función crea dos listas en una se colocan todos los números entre
# el número "base" y el número 2, por otro lado se crea una lista en la
# que se va colocando los números múltiplos de numeros anteriores, luego
# los números que se repitan en las listas se eliminan dejando únicamente
# los números primos

def numero_primo(base):
    primos = []
    no_primos = []
    if not isinstance(base, int):
        return -64, None
    if isinstance(base, bool):
        return -64, None
    if base > 100:
        return -80, None
    for i in range(base, 1, -1):
        primos.append(i)
    primos.reverse()
    for n in primos:
        if n * n <= base:
            for i in range(1, base + 1):
                if i % n == 0 and i != n:
                    no_primos.append(i)
    for i in no_primos:
        if i in primos:
            primos.remove(i)
    return 0, primos
